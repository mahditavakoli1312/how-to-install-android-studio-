# راهنمای نصب Android Studio و اضافه کردن مخزن Myket Maven

این فایل قدم‌به‌قدم توضیح می‌دهد چطور Android Studio را نصب کنید و قبل از این‌که به شکل جدی با Gradle و دانلود وابستگی‌ها کار کنید، **مخزن Myket Maven** را به پروژه اضافه کنید تا دانلود کتابخانه‌ها راحت‌تر و پایدارتر باشد.

> ⚠️ نکته مهم  
> دانلود خود **Android SDK** (آن چیزی که در Setup Wizard دانلود می‌شود) از سرورهای گوگل انجام می‌شود و ربطی به Maven ندارد.  
> مخزن Myket فقط برای **کتابخانه‌ها و پلاگین‌هایی** است که Gradle در زمان Sync و Build دانلود می‌کند.

---

## ۱. دانلود Android Studio

1. مرورگر را باز کنید.
2. به سایت رسمی Android Studio بروید:  
   `https://developer.android.com/studio`
3. جدیدترین نسخه Android Studio را متناسب با سیستم‌عامل خود (Windows / macOS / Linux) دانلود کنید.

---

## ۲. نصب Android Studio

### روی Windows

1. فایل نصبی با پسوند `.exe` را اجرا کنید.
2. روی **Next** بزنید تا مراحل نصب جلو برود.
3. مسیر نصب را در صورت نیاز تغییر دهید.
4. در پایان روی **Finish** کلیک کنید تا Android Studio اجرا شود.

### روی macOS

1. فایل `.dmg` را باز کنید.
2. آیکون Android Studio را به پوشه‌ی `Applications` بکشید.
3. از پوشه `Applications`، Android Studio را اجرا کنید.

### روی Linux

1. فایل `.tar.gz` را از حالت فشرده خارج کنید.
2. داخل پوشه `bin`، فایل `studio.sh` را اجرا کنید.
3. برای راحتی، می‌توانید Launcher هم بسازید (خود Android Studio معمولاً پیشنهاد می‌دهد).

---

## ۳. اجرای اولیه و نصب SDK

1. اولین بار که Android Studio را باز می‌کنید، **Setup Wizard** باز می‌شود.
2. روی **Next** بزنید و:
   - نوع نصب را روی **Standard** بگذارید (برای شروع مناسب است).
3. در مرحله‌ی **SDK Components**، نسخه‌ی پیش‌فرض Android SDK (مثلاً Android 14 یا 15) را بپذیرید.
4. روی **Next** و بعد **Finish** بزنید تا **SDK و ابزارهای لازم** دانلود شوند.

> 📌 بدون دانلود اولیه SDK، امکان ساخت و اجرای پروژه وجود ندارد،  
> اما برای دانلود بقیه لایبرری‌ها و وابستگی‌ها می‌توانیم از Myket Maven کمک بگیریم.

---

## ۴. ساخت اولین پروژه

بعد از نصب اولیه و دانلود SDK:

1. در Android Studio روی **New Project** کلیک کنید.
2. یک Template ساده مثل **Empty Activity** را انتخاب کنید.
3. نام پروژه، Package Name و مسیر ذخیره را تنظیم کنید.
4. روی **Finish** کلیک کنید تا پروژه ساخته شود.

Android Studio حالا یک پروژه‌ی نمونه برای شما می‌سازد.

---

## ۵. اضافه کردن مخزن Myket Maven قبل از Gradle Sync جدی

حالا قبل از این‌که شروع به اضافه کردن لایبرری‌های مختلف کنید، بهتر است مخزن Myket Maven را به پروژه اضافه کنید تا در Gradle Sync بعدی، از همین مخزن هم استفاده شود.

### ۵.۱. اگر پروژه شما از Gradle Kotlin DSL استفاده می‌کند (`.kts`)

1. فایل **`settings.gradle.kts`** را در ریشه پروژه باز کنید.
2. بخش `dependencyResolutionManagement` را پیدا کنید.
3. داخل بلاک `repositories` کد زیر را قرار دهید (یا به آن اضافه کنید):

```kotlin
dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()

        // مخزن Myket Maven
        maven {
            url = uri("https://maven.myket.ir")
        }

        // فقط اگر واقعاً نیاز داری:
        jcenter()
    }
}
```

فایل را ذخیره کنید و در بالای صفحه روی **Sync Now** کلیک کنید.

---

### ۵.۲. اگر پروژه شما از Gradle Groovy استفاده می‌کند (`build.gradle` معمولی)

1. فایل **`build.gradle`** سطح روت (Root) را باز کنید.
2. دنبال بلاک زیر بگردید (یا مشابه آن):

```groovy
allprojects {
    repositories {
        google()
        mavenCentral()
        // ...
    }
}
```

3. مخزن Myket Maven را به این بلاک اضافه کنید:

```groovy
allprojects {
    repositories {
        google()
        mavenCentral()

        // مخزن Myket Maven
        maven {
            url "https://maven.myket.ir"
        }

        // فقط اگر به لایبرری‌های قدیمی نیاز دارید:
        jcenter()
    }
}
```

با زدن **Sync Now** در Android Studio، Gradle از این به بعد می‌تواند بخشی از وابستگی‌ها را از Myket Maven دریافت کند.

---

## ۶. تست با یک لایبرری ساده

برای اینکه مطمئن شوید همه‌چیز درست کار می‌کند:

1. به فایل `build.gradle` (یا `build.gradle.kts`) ماژول `app` بروید.
2. در بخش `dependencies` یک لایبرری ساده اضافه کنید مثلاً (نمونه):

```kotlin
dependencies {
    implementation("androidx.core:core-ktx:1.13.1")
}
```

3. روی **Sync Now** کلیک کنید.
4. اگر Gradle بدون خطا Sync شد، تنظیمات شما درست است.

---

## ۷. نکات مهم

- اگر سرعت دانلود کم یا قطع و وصل زیاد بود، می‌توانید دوباره Sync کنید؛ Myket Maven کمک می‌کند بخشی از مسیر دانلود وابستگی‌ها پایدارتر باشد.
- **`jcenter()`** قدیمی و deprecate شده است؛ فقط اگر یک لایبرری خاص مجبور می‌کند، از آن استفاده کنید.
- اگر پروژه قدیمی دارید که قبلاً ساخته شده، کافی است همین تنظیم Myket Maven را به آن هم اضافه کنید و دوباره Sync کنید.
- این راهنما برای همه اعضای تیم یکسان است؛ کافی است هر کس Android Studio را نصب کند و طبق مرحله ۵ مخزن Myket Maven را اضافه کند.
